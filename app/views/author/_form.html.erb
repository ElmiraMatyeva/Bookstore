<%= simple_form_for @author, html: { multipart: true } do |f| %>
	  <div class="form-group text-center">
          <%= f.text_field :name, label: 'Имя', class: 'form-control'%>
        </div>
          <div class="form-group text-center">
          <%= f.submit class: 'btn-success btn-block' %>
        </div>
<% end %>